<h3 class="panel-title"><b>{{ text_vault }}</b></h3>

<div class="form-group">
    <label class="col-sm-2 control-label" for="input-vault">{{ config_vault }}</label>
    <div class="col-sm-10">
        <select name="{{ prefix ~ 'vault' }}" id="input-vault" class="form-control">
            {% if vault %}
                <option value="1" selected="selected">{{ text_enabled }}</option>
                <option value="0">{{ text_disabled }}</option>
            {% else %}
                <option value="1">{{ text_enabled }}</option>
                <option value="0" selected="selected">{{ text_disabled }}</option>
            {% endif %}
        </select>
    </div>
    <div class="form-group">
        <div class="col-sm-2"></div>
        <div class="col-sm-10 description">
            <i>{{ config_vault_desc }}</i>
        </div>
    </div>
</div>

<div class="form-group">
    <label class="col-sm-2 control-label" for="input-vault">{{ config_allow_changed_shipping }}</label>
    <div class="col-sm-10">
        <select name="{{ prefix ~ 'allow_changed_shipping' }}" id="input-secure-shipping-address" class="form-control">
            {% if allow_changed_shipping %}
                <option value="1" selected="selected">{{ text_enabled }}</option>
                <option value="0">{{ text_disabled }}</option>
            {% else %}
                <option value="1">{{ text_enabled }}</option>
                <option value="0" selected="selected">{{ text_disabled }}</option>
            {% endif %}
        </select>
    </div>
    <div class="form-group">
        <div class="col-sm-2"></div>
        <div class="col-sm-10 description">
            <i>{{ config_allow_changed_shipping_desc }}</i>
        </div>
    </div>
</div>